import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rozzy87.gearcalc',
  appName: 'Mini lathe change gear calculator',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
